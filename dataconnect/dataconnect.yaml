specVersion: "v1"
serviceId: "cookies"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cookies-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
